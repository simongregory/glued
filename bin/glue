#!/usr/bin/env ruby

$:.push File.join(File.dirname(__FILE__), "..", "lib")
$:.push File.dirname(__FILE__)

require 'glue'

puts %{Ruby HDS Glue #{Glue::VERSION}

Fetch it, glue it, watch it.

}

begin
  Glue.new(ARGV[0])
rescue Exception => e
  abort "Glue failed because: #{e.message}"
end
